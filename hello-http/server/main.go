package main

func main() {
	// endpoint := "127.0.0.1:50052"

	// creds, err := credentials.NewClientTLSFromFile("../keys", "../keys")
	// if err != nil {
	// 	grpclog.Fatalf("Failed to generate credentials %v", err)
	// }
	// conn, _ := net.Listen("tcp", endpoint)
	// grpcServer := grpc.NewServer(grpc.Creds(creds))

}
